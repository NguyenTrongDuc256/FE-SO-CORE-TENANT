/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FormatTimeToMinutePipe } from './format-time-to-minute.pipe';

describe('Pipe: FormatTimeToMinutee', () => {
  it('create an instance', () => {
    let pipe = new FormatTimeToMinutePipe();
    expect(pipe).toBeTruthy();
  });
});
